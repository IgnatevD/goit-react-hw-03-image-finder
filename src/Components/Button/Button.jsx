const Button = () => {
  return (
    <button type="button" className="Button">
      Загрузить еще
      {/* window.scrollTo({
  top: document.documentElement.scrollHeight,
  behavior: 'smooth',
}); */}
    </button>
  );
};

export default Button;
