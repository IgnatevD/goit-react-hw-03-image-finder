const Button = () => {
    return (
        <button className="Button">  кнопка
{/* window.scrollTo({
  top: document.documentElement.scrollHeight,
  behavior: 'smooth',
}); */}
        </button>
    );
}

export default Button;